# Logging - optional but useful
# log-file=stdout
# verbose
# debug
# log-binding

# STUN-only mode
stun-only

# Don't expose an admin CLI
no-cli

# STUN Ports
listening-port=3478
alt-listening-port=3479

# Listening on both public IPs (RFC 5780 requires same-family dual IPs)
listening-ip=YOUR_PRIMARY_PUBLIC_IP
listening-ip=YOUR_SECONDARY_PUBLIC_IP

# Turn relay IPs (you can leave these out when using `stun-only`, but optional)
# relay-ip=YOUR_PRIMARY_PUBLIC_IP
# relay-ip=YOUR_SECONDARY_PUBLIC_IP

# Optional port range (if you plan to expand TURN later)
min-port=5730
max-port=5740

# Enables NAT discovery (RFC 5780).
# NOTE: Coturn 4.5.2 (instrumentisto/coturn) logs "Bad configuration format: rfc5780",
# so keep it disabled unless you upgrade coturn.
# rfc5780
